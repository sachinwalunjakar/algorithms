#include <bits/stdc++.h>
#include "../../print.cpp"
using namespace std;


int main() {
  // creating & intializing 2d vector // 10 * 10 matrix with default value -1
  vector<vector<int>> matrix2d(10, vector<int>(10, -1));
  // inserting multiple element in vector
  vector<int> a; a.insert(a.end(), {1,2,3,4,5,6});
  return 0;
}